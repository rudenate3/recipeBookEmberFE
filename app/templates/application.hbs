<div class="container">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    {{# link-to "index" class="navbar-brand"}}Recipe Book{{/link-to}}
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          {{# link-to "index" class="nav-link"}}Home{{/link-to}}
        </li>
        <li class="nav-item active">
          {{# link-to "recipes" class="nav-link"}}Recipes{{/link-to}}
        </li>
        <li class="nav-item active">
          {{# link-to "ingredients" class="nav-link"}}Ingredients{{/link-to}}
        </li>
      </ul>
      <div>
        {{# link-to "sign-in" class="btn btn-outline-light"}}Login{{/link-to}}
      </div>
    </div>
  </nav>

  <div>
    {{outlet}}
  </div>
</div>